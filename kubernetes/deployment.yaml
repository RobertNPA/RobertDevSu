apiVersion: apps/v1
kind: Deployment
metadata:
  name: devsu-node-app
  labels:
    app: devsu-node-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devsu-node-app
  template:
    metadata:
      labels:
        app: devsu-node-app
    spec:
      containers:
      - name: devsu-node-app
        image: devsu-node-app:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: devsu-app-config
        - secretRef:
            name: devsu-app-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: devsu-node-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: devsu-node-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80 